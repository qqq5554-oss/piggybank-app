# 本機開發用。複製這個檔案為 .env，貼上你的 Neon 連線字串。
#
# ⚠️ 這組是完整資料庫帳密，只給後端 API（/api 資料夾）使用，
# 絕對不能加上 VITE_ 前綴，否則會被打包進前端程式碼、暴露給所有人看到。
#
# 到 Neon 專案首頁 > Connection string 複製整串（含 ?sslmode=require）

DATABASE_URL=postgresql://user:password@ep-xxxx.aws.neon.tech/neondb?sslmode=require
